// this is the main file for the progra

#include <stdio.h>
#include "math_utils.h"   // Include our function header

int main() {
    int result = addTwoNumbers(3, 5);
    printf("Result: %d\n", result);
    return 0;
}

